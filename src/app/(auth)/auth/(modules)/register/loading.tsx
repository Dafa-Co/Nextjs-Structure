import LoadingComponent from "@/container/components/loading";

type Props = {};

export default function Loading({}: Props) {
  return (
    <section>
      <LoadingComponent />
    </section>
  );
}
